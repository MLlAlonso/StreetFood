import { StyleSheet } from "react-native";

export default StyleSheet.create({

    input: {
        height: 48,
        borderWidth: 1,
        borderColor: "#DDD",
        borderRadius: 10,
        paddingHorizontal: 12,
        fontSize: 16,
        backgroundColor: "#FFF",
    },

});